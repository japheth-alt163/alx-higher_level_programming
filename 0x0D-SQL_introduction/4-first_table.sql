-- Create the 'first_table' if it does not already exist
CREATE TABLE IF NOT EXISTS first_table (
    id INT,               -- Integer column for unique identifiers
    name VARCHAR(256)     -- Variable character column with a maximum length of 256 characters
);
